#include "main.h"



void auto_drive_circle(double rad, double time, int &step) {
  switch (step) {
    case 0 :
    starting_y = GlobalPosition.y;
    starting_x = GlobalPosition.x;
    break;
  }
}
